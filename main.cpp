#include "compressing_plane.h"
#include "func.h"
int main()
{
	CompressingPlane A;
	list<MatrixElement> l =  stringMatrix(1,1,1);
	// for (int i = 0; i < n; ++i)
	// {
	// 	for (int j = 0; j < n; ++j)
	// 	{
	// 		matrix.push_back(stringMatrix(i,j,n));//получение строки занчений
	// 	}
	// }
	return 0;
}
